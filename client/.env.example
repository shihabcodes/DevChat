# Client Environment Variables

# API URL mapping
# For local dev without Vercel Edge proxies, you can leave this blank (defaults to http://localhost:5001/api).
# For production deployment (Vercel), point this to your full production backend URL.
NEXT_PUBLIC_API_URL=http://localhost:5001/api
